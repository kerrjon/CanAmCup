@model IEnumerable<CanAmCup.Models.Score>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Holes.HoleId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Matches.CourseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoleWinner)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Holes.HoleId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Matches.CourseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoleWinner)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
